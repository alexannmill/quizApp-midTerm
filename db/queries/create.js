const db = require('../connection')


// allq's need to be made before quiz (FK)
const createQuestion = () => {
  return db.query(``)
  .then()
};
exports.createQuestion = createQuestion

//
const createQuiz = () => {
  return db.query(``)
  .then()
}
exports.createQuiz = createQuiz


