<head>
  <link rel="stylesheet" href="/styles/_header.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com/%22%3E" />
  <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Oxygen:wght@300;400;700&family=Stalinist+One&display=swap"
    rel="stylesheet">
</head>
<header>
  <nav class="navbar">
    <a class="navbar-brand" href="/">Quizz App</a>
    <div class="navbar-items" id="navbar-paths">
      <a id="create-item" href="/create">Create</a>
      <a id="results-item" href="/results">Results</a>
    </div>
    <div class="logReg">
      <!-- logged-out -->
      <!-- will need div -->
    <% if (userID) { %>
      <form method="POST" action="/logout">
        <button name="logout" type="submit" class="logout">Log-out</button>
      </form>
    <% } else { %>
      <!--logged-in-->
      <form method="POST" action="/login">
        <textarea name="name">  </textarea>
        <div class="buttons">
          <button name="login" type="submit" class="login" value="login">Login</button>
          <button name="login" type="submit" class="register" value="register">Register</button>
        </div>
      </form>
    <% } %>
    </div>
  </nav>
</header>
